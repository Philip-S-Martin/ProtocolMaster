<UserControl x:Class="ProtocolMasterWPF.View.TitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProtocolMasterWPF.View" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.ColumnSpan="3" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0 0 0 1"/>
        <Grid x:Name="AppTitleBar" MouseDown="AppTitleBar_MouseDown" Background="Transparent" Grid.Column="0" Grid.ColumnSpan="3">
        </Grid>
        <Menu Grid.Column="0" Margin="2,2,0,1">
            <MenuItem Header="Item 1">
                <MenuItem Header="Sub Item 1" />
                <MenuItem Header="Sub Item 2" />
                <MenuItem Header="Sub Item 3" />
            </MenuItem>
            <MenuItem Header="Item 2">
                <MenuItem Header="Sub Item 1" />
                <MenuItem Header="Sub Item 2" />
                <MenuItem Header="Sub Item 3" />
            </MenuItem>
            <MenuItem Header="Item 3">
                <MenuItem Header="Sub Item 1" />
                <MenuItem Header="Sub Item 2" />
                <MenuItem Header="Sub Item 3" />
            </MenuItem>

        </Menu>
        <StackPanel Grid.Column="2" Orientation="Horizontal">
            <Button Style="{StaticResource MaterialDesignIconButton}" Foreground="{StaticResource MaterialDesignDarkForeground}" Margin="2,2">
                <materialDesign:PackIcon Kind="WindowMinimize"/>
            </Button>
            <Button Style="{StaticResource MaterialDesignIconButton}" Foreground="{StaticResource MaterialDesignDarkForeground}" Margin="2,2">
                <materialDesign:PackIcon Kind="WindowMaximize"/>
            </Button>
            <Button Style="{StaticResource MaterialDesignIconButton}" Foreground="{StaticResource MaterialDesignDarkForeground}" Margin="2,2">
                <materialDesign:PackIcon Kind="WindowClose"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
